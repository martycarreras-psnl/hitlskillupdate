# Production build environment (used by `vite build` → `pac code push`).
# Flips the provider factory to the real Dataverse provider for the deployed app.
# Local dev (`npm run dev`) and tests stay on the mock provider unless overridden.
VITE_USE_MOCK=false
